<ContentDialog
    x:Class="RecipeBrowser.DataImportResultDialog"
    x:Name="DataImportResultDialog"
    x:Uid="DataImportResultDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    SecondaryButtonText="">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0"  Grid.Row="0" x:Uid="CaloricInfoUpdates"/>
        <TextBlock Grid.Column="1"  Grid.Row="0" Text="{x:Bind CaloricInfoUpdates}" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="0"  Grid.Row="1" x:Uid="RecipeTagsUpdates"/>
        <TextBlock Grid.Column="1"  Grid.Row="1" Text="{x:Bind RecipeTagsUpdates}" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="0"  Grid.Row="2" x:Uid="NewTagDirectoryEntries"/>
        <TextBlock Grid.Column="1"  Grid.Row="2" Text="{x:Bind NewTagDirectoryEntries}" HorizontalAlignment="Right"/>
    </Grid>
</ContentDialog>

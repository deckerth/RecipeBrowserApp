<ContentDialog
    x:Uid="SettingsDialog"
    x:Class="RecipeBrowser.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TITLE"
    PrimaryButtonText="OK"
    SecondaryButtonText=""
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    IsSecondaryButtonEnabled="False">
    <Grid>
        <StackPanel>
            <ComboBox x:Name="ThemeComboBox" x:Uid="ThemeComboBox">
                <ComboBoxItem x:Name="ThemeLight" x:Uid="ThemeLight"/>
                <ComboBoxItem x:Name="ThemeDark" x:Uid="ThemeDark"/>
            </ComboBox>
            <ToggleSwitch x:Uid="ZoomButtonsSwitch" x:Name="ZoomButtonsSwitch" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,20,0,0"/>
            <Button x:Name="ChangeRootFolder" x:Uid="ChangeRootFolder" Content="Startordner ändern" Margin="0,20,0,0"/>
            <ToggleSwitch x:Uid="LoggingSwitch" x:Name="LoggingEnabledSwitch" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</ContentDialog>
